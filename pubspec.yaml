name: fin_pessoal
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.30.0
  drift_flutter: ^0.2.8
  flutter_riverpod: ^2.6.1
  path_provider: ^2.1.5
  path: ^1.9.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  drift_dev: ^2.30.0

flutter:
  uses-material-design: true
